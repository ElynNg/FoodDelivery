name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm install

    - name: Build
      run: npm run build

    - name: Deploy
      run: |
        git config --global user.name Eleanor Nguyen
        git config --global user.email 74583218+ElynNg@users.noreply.github.com
        git remote set-url origin git@github.com:ElynNg/FoodDelivery.git
        npm run deploy
      env:
        user_name: 'Eleanor Nguyen'
        user_email: '74583218+ElynNg@users.noreply.github.com'
        github_token: 'secrets.ELYN_GH_TOKEN'
        repository: 'FoodDelivery'
